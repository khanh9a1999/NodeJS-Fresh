# NodeJS API

Express + TypeScript API scaffold (layered structure).

## Run

```bash
pnpm dev
```

## Endpoints

- `GET /api/v1/users` — list users
- `POST /api/v1/users` — create user (`name`, `email`)
